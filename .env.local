# Judge0 API Configuration (# Code Execution - Uses browser-based parallel execution (no external API needed)

# WebSocket Configuration
VITE_WEBSOCKET_URL=http://localhost:3001
VITE_APP_URL=http://localhost:5173
WEBSOCKET_PORT=3001

# Supabase Configuration (already exists, keep your current values)
# VITE_SUPABASE_URL=your_supabase_url
# VITE_SUPABASE_ANON_KEY=your_supabase_key
